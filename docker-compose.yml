services:
  flask-app:
    build: .
    ports:
      - "8000:5000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST = redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:

        